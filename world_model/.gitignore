# Editable-install build metadata
*.egg-info/

# Python caches
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime artifacts (per spec Section 9.1)
# /data/ means only the top-level data/ dir (replay buffer storage), not pokemon_planner/data/ or docs/data/
/data/
runs/
